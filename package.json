{
  "name": "git-tracker-client",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "start": "NODE_ENV=development electron .",
    "watch": "NODE_ENV=development nodemon --exec npm start",
    "tsc-watch":"tsc && tsc --watch",
    "client-dev":"cd interface && npm start"
  },
  "author": "Roei Bracha",
  "license": "MIT",
  "devDependencies": {
    "electron": "^9.4.0",
    "nodemon": "^2.0.2"
  },
  "dependencies": {
    "git-utils": "^5.6.2",
    "simple-git": "^1.131.0"
  }
}
